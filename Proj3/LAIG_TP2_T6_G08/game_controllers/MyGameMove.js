class MyGameMove {
  constructor(scene, gameController, orig, dest, piece) {
    this.scene = scene;
    this.gameController = gameController;
    this.orig = orig;
    this.dest = dest;
    this.piece = piece;
  }
}
